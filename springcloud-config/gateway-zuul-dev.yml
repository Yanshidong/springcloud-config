zuul:
  routes:
    api-a:
      path: /demo/**
      serviceId: demo-client
    api-b:
      path: /ribbon/**
      serviceId: balance-ribbon
    api-c:
      path: /auth/**
      serviceId: oauth-center